# Installed as ~/.config/jiko/kiosk.env for the graphical kiosk user.
# JIKO_KIOSK_URL may select another loopback-built page, but the readiness probe
# intentionally rejects a remote host.
JIKO_KIOSK_URL=http://127.0.0.1:4173/?mode=device
